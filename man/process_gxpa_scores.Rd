% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare_scores.R
\name{process_gxpa_scores}
\alias{process_gxpa_scores}
\title{Process and Export Score Files for GXPA}
\usage{
process_gxpa_scores(
  deg_meta,
  gene_id_map,
  input_dir,
  output_dir,
  score_thresholds = list(logFC = 1, FDR = 0.05)
)
}
\arguments{
\item{deg_meta}{A data frame containing score file metadata. Required columns:
\code{score_file_name}, \code{name}, \code{description}, \code{logfc_column}, \code{fdr_column}, \code{group1}, \code{group2}.}

\item{gene_id_map}{A data frame mapping original gene IDs to symbols. Must contain
\code{gene_id} (matching original score files) and \code{gene_symbol} (final output names).}

\item{input_dir}{Directory containing the raw score files.}

\item{output_dir}{Directory where processed score files will be written.}

\item{score_thresholds}{A list with \code{logFC} and \code{FDR} elements for calculating rank thresholds.
Default \code{list(logFC = 1, FDR = 0.05)}.}
}
\value{
A data frame containing the generated score info records.
}
\description{
Filters and ranks DEG score files, then writes them to the output directory
and generates the \code{expr.score_info.csv} file.
}
